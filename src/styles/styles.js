import styled from 'styled-components';
import {Alert} from 'react-bootstrap';

const HeaderLine = styled.span`
  border-bottom: 3px solid black;
`
const Image = styled.img`
  margin: 5px;
	width: 2em;
	
`;

export {HeaderLine, Image };